{% extends 'base.html.twig' %}

{% block title %}ArticleBoutique index{% endblock %}

{% block body %}
    <h1>ArticleBoutique index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Type</th>
                <th>Prix</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for article_boutique in article_boutiques %}
            <tr>
                <td>{{ article_boutique.id }}</td>
                <td>{{ article_boutique.nom }}</td>
                <td>{{ article_boutique.type }}</td>
                <td>{{ article_boutique.prix }}</td>
                <td>
                    <a href="{{ path('app_article_boutique_show', {'id': article_boutique.id}) }}">show</a>
                    <a href="{{ path('app_article_boutique_edit', {'id': article_boutique.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_article_boutique_new') }}">Create new</a>
{% endblock %}
