{% extends 'base.html.twig' %}

{% block title %}Edit ParticipantEvenement{% endblock %}

{% block body %}
    <h1>Edit ParticipantEvenement</h1>

    {{ include('participant_evenement/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_participant_evenement_index') }}">back to list</a>

    {{ include('participant_evenement/_delete_form.html.twig') }}
{% endblock %}
