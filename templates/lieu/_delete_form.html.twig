<form method="post" action="{{ path('app_lieu_delete', {'lieuId': lieu.lieuId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lieu.lieuId) }}">
    <button class="btn">Delete</button>
</form>
