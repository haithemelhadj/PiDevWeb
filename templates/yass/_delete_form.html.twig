<form method="post" action="{{ path('app_yass_delete', {'id': yass.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ yass.id) }}">
    <button class="btn">Delete</button>
</form>
