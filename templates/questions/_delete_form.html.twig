<form method="post" action="{{ path('app_questions_delete', {'qid': question.qid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ question.qid) }}">
    <button class="btn">Delete</button>
</form>
